/* Inspired by Redux. Super simple API for updating and fetchin
 * application state. Reasoning is to keep application state separate
 * from application logic.
*/
const createStore = () => {
  return {};
}

module.exports = createStore;
